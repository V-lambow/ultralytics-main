# run_yolo26_train.py 参数详解

## 1. `--model`（默认 `./models/yolo26m.pt`）

模型权重文件。可选 `n/s/m/l/x` 五种尺寸，越大越准但越慢。

| 场景 | 建议 |
|------|------|
| 快速验证流程是否跑通 | `yolo26n.pt`（最快，精度最低） |
| 平衡速度与精度（推荐起步） | `yolo26m.pt` |
| 追求最高精度，训练时间充裕 | `yolo26l.pt` 或 `yolo26x.pt` |
| 从零训练（不用预训练） | 传一个空的 YAML 模型配置文件 |

## 2. `--data`（默认 `coco8.yaml`）

数据集配置文件，定义图片路径、类别数和类别名。

## 3. `--epochs`（默认 `100`）

训练轮数。

| 场景 | 建议 |
|------|------|
| 快速调试 | 5~10 轮，确认流程没问题 |
| 数据集小（<1000 张） | 200~500 轮，观察 loss 收敛 |
| 数据集大（>5000 张）+ 预训练微调 | 100~300 轮通常够 |
| 从零训练（无预训练） | 500~1000+ 轮 |

设 1000 轮时，配合 `patience=100` 早停兜底 — 如果 100 轮没提升就自动停，不会真跑满 1000 轮。

## 4. `--batch`（默认 `16`）

每步送入 GPU 的图片数。直接影响显存占用和训练稳定性。

| 场景 | 建议 |
|------|------|
| 显存 8GB | `batch=8` 或 `batch=16`（配合 `imgsz=640`） |
| 显存 12~16GB | `batch=16` 或 `batch=32` |
| 显存 24GB+ | `batch=32` 或 `batch=64` |
| 显存不够报 OOM | 降低 batch，或降低 `imgsz` |
| 想要更大 batch 但显存不足 | 设 `batch=-1`，自动开启 AutoBatch，自动找最大 batch |

**注意**：`nbs=64`（默认），实际学习率会按 `batch / nbs` 比例缩放，所以改 batch 不需要手动调 lr。

## 5. `--imgsz`（默认 `640`）

训练输入图片尺寸（正方形，短边 resize 到此值）。

| 场景 | 建议 |
|------|------|
| 通用检测 | `640`（标准，绝大多数场景够用） |
| 目标很小（小零件、远距离） | `1280`，大幅提升小目标检测能力 |
| 目标很大 / 想加快训练 | `320` 或 `480`，速度更快但丢失细节 |
| 显存不足 | 降低 imgsz 比降低 batch 更省显存（显存与 imgsz² 成正比） |

## 6. `--device`（默认自动选 GPU 0 或 CPU）

指定训练设备。

| 场景 | 建议 |
|------|------|
| 单 GPU | `"0"` |
| 多 GPU 并行 | `"0,1,2,3"`（自动 DDP） |
| 仅 CPU | `"cpu"`（极慢，仅调试用） |
| 指定特定 GPU | `"1"`（如果有多卡，选择显存更大的那张） |

## 7. `--workers`（默认 `8`）

数据加载线程数，影响数据预处理速度。

| 场景 | 建议 |
|------|------|
| Windows | `4~8`（脚本已自动限制 CPU 时 ≤4） |
| Linux 服务器 | `8~16` |
| 数据加载成为瓶颈（GPU 利用率低） | 增大 workers |
| CPU 内存不足 | 降低 workers |

## 8. `--project`（默认 `runs/detect`）/ `--name`（默认 `train`）

训练结果保存位置，即 `runs/detect/train/`、`runs/detect/train2/` 等。

| 场景 | 建议 |
|------|------|
| 不同超参实验对比 | 改 `--name` 区分，如 `--name experiment_v1` |
| 不想每次自增目录 | 加 `--exist-ok` 覆盖同名目录 |

## 9. `--exist-ok`（默认 False）

是否允许覆盖已有的 `project/name` 目录。重复实验时加这个避免生成 `train2`、`train3`...

## 10. `--pretrained`（默认 `True`）

是否使用预训练权重。传 `.pt` 文件时通常保持 `True`，模型会加载预训练 backbone。

| 场景 | 建议 |
|------|------|
| 微调（推荐） | `True`，利用预训练特征 |
| 从零训练（无预训练） | `False`，完全随机初始化 |

## 11. `--optimizer`（默认 `auto`）

优化器选择。

| 场景 | 建议 |
|------|------|
| 默认 | `auto`（ultralytics 自动选，通常是 AdamW） |
| 小数据集 / 需要更稳定 | `AdamW` |
| 大数据集 / 追求泛化 | `SGD`（需要更多 epoch 但泛化可能更好） |

## 12. `--lr0`（默认 `0.01`）

初始学习率。

| 场景 | 建议 |
|------|------|
| 默认微调 | `0.01`（SGD）/ `0.001`（Adam） |
| 训练震荡不收敛 | 降低，如 `0.001` |
| 训练太慢 | 适当提高 |
| 从零训练 | `0.01` 配合 SGD |

**注意**：`auto` 优化器会自动调整，一般不用手动改。

## 13. `--patience`（默认 `100`）

早停耐心值：连续 N 个 epoch mAP 不提升就停止训练。

| 场景 | 建议 |
|------|------|
| 快速实验 | `20~30`，节省时间 |
| 正式训练 | `100`（默认） |
| 数据集噪声大，指标波动 | `150~200`，避免过早停止 |

## 14. `--resume`（默认 False）

从上次中断的 checkpoint 续训。

| 场景 | 建议 |
|------|------|
| 训练被打断（断电/显存不足） | `--resume` |
| 从零开始新训练 | 不加此参数 |

## 15. `--amp` / `--no-amp`

混合精度训练（FP16），加速 + 省显存。

| 场景 | 建议 |
|------|------|
| 有 GPU（默认开启） | 保持开启，速度提升 ~50%，几乎不掉精度 |
| 训练出现 NaN / loss 异常 | `--no-amp` 关闭排查 |
| CPU 训练 | 自动关闭 |

## 16. `--cache`（默认 `False`）

缓存图片到内存/磁盘，加速数据加载。

| 场景 | 建议 |
|------|------|
| 数据集小（<5GB）+ 内存充足 | `--cache ram`，首次加载后极快 |
| 数据集大 + 磁盘快（SSD） | `--cache disk` |
| 内存/磁盘不足 | `False`（默认） |

## 17. `--task`（默认 `detect`）

任务类型，可选 `detect`、`segment`、`classify`、`pose`、`obb`。通常自动推断，不用改。

## 18. `--plots`（默认 True）

保存训练曲线、混淆矩阵、PR 曲线等可视化。正式训练保持开启。

## 19. `--save-period`（默认 `-1`）

每隔 N 个 epoch 保存一次 checkpoint。`-1` 表示只保存最佳和最后一次。

| 场景 | 建议 |
|------|------|
| 长时间训练（>500 epoch） | `--save-period 50`，方便回溯 |
| 短训练 | `-1` 就够 |

## 20. `--cfg`（默认 None）

加载额外的超参配置 YAML，可以一次性覆盖所有训练参数。

## 21. `--seed`（默认 `0`）

随机种子。`0` = 每次训练结果略有不同。

| 场景 | 建议 |
|------|------|
| 需要可复现 | 设一个固定值如 `--seed 42` |
| 多次训练取最优 | 保持 `0`，跑多次看平均表现 |

## 22. `--cos-lr`（默认 False）

余弦退火学习率调度器，替代默认的线性衰减。

| 场景 | 建议 |
|------|------|
| 训练后期想更精细地收敛 | `--cos-lr` |
| 默认线性衰减 | 不加此参数（够用） |

## 23. `--close-mosaic`（默认 `10`）

最后 N 个 epoch 关闭 mosaic 增强。

| 场景 | 建议 |
|------|------|
| 默认 | `10`（最后 10 轮关闭，让模型在正常图片上微调） |
| 目标排列密集 | `15~20`，更早关闭让模型学正常布局 |
| 从零训练 | `20~30`，给模型更多纯净样本收敛 |
