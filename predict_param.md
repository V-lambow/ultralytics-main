# run_yolo26_predict.py 参数详解

## 1. `--model`（默认 `yolo26n.pt`）

推理使用的模型权重文件。

| 场景               | 建议                                |
| ------------------ | ----------------------------------- |
| 快速测试流程       | `yolo26n.pt`（最快）                |
| 高精度推理         | `yolo26m.pt` / `yolo26l.pt`         |
| 使用自己训练的模型 | `runs/detect/train/weights/best.pt` |

## 2. `--source`（默认 `ultralytics/assets/bus.jpg`）

推理输入源，支持多种格式。

| 场景                   | 建议                                    |
| ---------------------- | --------------------------------------- |
| 单张图片               | `path/to/image.jpg`                     |
| 图片文件夹（批量推理） | `path/to/folder/`                       |
| 视频文件               | `path/to/video.mp4`                     |
| 摄像头实时推理         | `0`（默认摄像头）或 `1`（第二个摄像头） |
| 网络视频流 / RTSP      | `rtsp://...` 或 `http://...`            |
| 截屏推理               | `screen`                                |

## 3. `--imgsz`（默认 `640`）

推理时的输入图片尺寸。

| 场景                | 建议                        |
| ------------------- | --------------------------- |
| 通用检测            | `640`（与训练一致即可）     |
| 目标很小            | `1280`，提升小目标召回率    |
| 目标很大 / 追求速度 | `320` 或 `480`              |
| 精度优先            | 与训练时的 `imgsz` 保持一致 |

**注意**：推理 imgsz 不需要与训练一致，但差距过大会影响精度。通常 `640` 通吃。

## 4. `--conf`（默认 `0.25`）

置信度阈值，低于此值的检测框会被过滤掉。

| 场景                   | 建议                             |
| ---------------------- | -------------------------------- |
| 通用场景               | `0.25`（默认，平衡召回与精确）   |
| 漏检多（召回不够）     | 降低到 `0.1` ~ `0.15`            |
| 误检多（精确不够）     | 提高到 `0.5` ~ `0.7`             |
| 工业质检（零容忍漏检） | `0.1` 甚至更低，宁可误检不可漏检 |
| 工业质检（零容忍误检） | `0.5` ~ `0.8`，只保留高置信结果  |

## 5. `--iou`（默认 `0.7`）

NMS（非极大值抑制）的 IoU 阈值，用于去除重叠框。

| 场景                          | 建议                               |
| ----------------------------- | ---------------------------------- |
| 通用场景                      | `0.7`（默认）                      |
| 目标密集重叠多                | 降低到 `0.5` ~ `0.6`，更激进地去重 |
| 目标稀疏 / 大量近似位置的目标 | 提高到 `0.8` ~ `0.9`，保留更多框   |

## 6. `--device`（默认自动选 GPU 0 或 CPU）

| 场景   | 建议                            |
| ------ | ------------------------------- |
| 单 GPU | `"0"`                           |
| 多 GPU | `"0,1,2,3"`                     |
| CPU    | `"cpu"`（实时推理不推荐，太慢） |

## 7. `--show`（默认 False）

是否弹出窗口实时显示推理结果。

| 场景              | 建议                       |
| ----------------- | -------------------------- |
| 调试 / 演示       | 加 `--show` 弹窗查看       |
| 批量推理 / 跑视频 | 不加（结果保存到文件即可） |

## 8. `--save`（默认 True）

是否保存推理结果图片/视频。默认开启，结果保存到 `runs/detect/predict/`。

## 9. `--save-txt`（默认 False）

是否将检测结果保存为 `.txt` 文件（YOLO 格式：`class_id x_center y_center width height`）。

| 场景                 | 建议            |
| -------------------- | --------------- |
| 需要后续处理检测结果 | 加 `--save-txt` |
| 只需要可视化         | 不加            |

## 10. `--save-conf`（默认 False）

保存 txt 结果时是否同时保存置信度分数。需配合 `--save-txt` 使用。

## 11. `--save-crop`（默认 False）

是否将每个检测框裁剪出来单独保存。

| 场景                     | 建议             |
| ------------------------ | ---------------- |
| 需要单独查看每个检测目标 | 加 `--save-crop` |
| 数据审核 / 标注复查      | 加 `--save-crop` |
| 常规推理                 | 不加（节省磁盘） |

## 12. `--project`（默认 `runs/detect`）/ `--name`（默认 `predict`）

推理结果保存位置。多次推理时改 `--name` 避免覆盖。

## 13. `--exist-ok`（默认 False）

是否允许覆盖同名目录。多次对同一来源推理时加这个。

## 14. `--classes`（默认 None，检测所有类别）

按类别 ID 过滤检测结果，只保留指定类别的目标。

| 场景           | 建议                                     |
| -------------- | ---------------------------------------- |
| 只关心特定类别 | `--classes 0 2 5`（只保留第 0、2、5 类） |
| 检测所有类别   | 不加此参数                               |

## 15. `--augment`（默认 False）

测试时增强（TTA），对图片做多尺度/翻转后多次推理再合并结果。

| 场景                | 建议                            |
| ------------------- | ------------------------------- |
| 追求最高精度        | 加 `--augment`，速度下降 2~3 倍 |
| 实时推理 / 速度优先 | 不加                            |
| 边界 case 验证      | 加，看模型极限表现              |

## 16. `--agnostic-nms`（默认 False）

类别无关的 NMS，不同类别的重叠框也会被抑制。

| 场景                                                          | 建议                       |
| ------------------------------------------------------------- | -------------------------- |
| 目标密集且不同类别经常重叠                                    | 加 `--agnostic-nms`        |
| 不同类别经常在同一位置（如同一人既是 person 又是 pedestrian） | 加                         |
| 通用场景                                                      | 不加（默认按类别分别 NMS） |

## 17. `--max-det`（默认 `300`）

每张图片最多保留的检测框数量。

| 场景                           | 建议                       |
| ------------------------------ | -------------------------- |
| 通用场景                       | `300`（默认够用）          |
| 目标极其密集（人群、大量零件） | 提高到 `500` ~ `1000`      |
| 目标稀疏                       | `100` 即可，减少后处理时间 |

## 18. `--vid-stride`（默认 `1`）

视频推理时的帧间隔，即每隔 N 帧推理一次。

| 场景                | 建议                |
| ------------------- | ------------------- |
| 实时视频            | `1`（每帧都推理）   |
| 视频很长 / 加速浏览 | `2` ~ `5`，跳帧加速 |
| 视频中目标变化慢    | `3` ~ `5`，节省算力 |
| 需要逐帧精确追踪    | `1`                 |

## 19. `--line-width`（默认 None，自动）

检测框的线宽。`None` 表示根据图片大小自动缩放。

| 场景                     | 建议             |
| ------------------------ | ---------------- |
| 默认                     | 不设（自动）     |
| 高分辨率输出需要更粗的框 | `--line-width 3` |
| 输出图片小，框太粗       | `--line-width 1` |

## 20. `--show-labels`（默认 True）/ `--show-conf`（默认 True）/ `--show-boxes`（默认 True）

控制可视化输出的内容：是否显示类别标签、置信度、检测框。通常保持默认。

| 场景                   | 建议                    |
| ---------------------- | ----------------------- |
| 纯检测框（不显示文字） | `--no-labels --no-conf` |
| 只看框不看标签         | `--no-labels`           |
| 干净的输出图片         | 关闭不需要的显示项      |

## 21. `--retina-masks`（默认 False）

分割任务时是否输出高分辨率 mask。

| 场景                 | 建议                |
| -------------------- | ------------------- |
| 分割任务需要精细边界 | 加 `--retina-masks` |
| 检测任务             | 无关，忽略          |

## 22. `--task`（默认 `detect`）

任务类型，通常自动推断，不用改。
